import { IteratorDirective } from './iterator.directive';

describe('IteratorDirective', () => {
  it('should create an instance', () => {
    const directive = new IteratorDirective();
    expect(directive).toBeTruthy();
  });
});
