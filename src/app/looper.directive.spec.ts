import { LooperDirective } from './looper.directive';

describe('LooperDirective', () => {
  it('should create an instance', () => {
    const directive = new LooperDirective();
    expect(directive).toBeTruthy();
  });
});
